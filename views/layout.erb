<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/bootstrap.min.css">

  <style>
    body {
      padding:2em 0 1em;
    }
    ul {
      list-style:none;
      padding:0;
      margin:0;
    }
    li {
      text-align:center;
      border-bottom:1px solid #F90;
      margin-bottom:1em;
    }
    p.paginate {
      text-align:center;
    }
    div.center {
      text-align:center;
      margin-bottom:4em;
    }
    div.center img {
      display:inline-block;
    }
    h2 {
      background:#ccc;
    }
    ul img {
      outline:1px solid #CCC;
      border:2px solid #444;
    }
    form {
      text-align: center;
      padding: 1em 0;
    }
    input {
      width:300px;
    }
    input, button {
      height:30px;
      border:1px solid #CCC;
      outline:none;
      display:inline-block;
      vertical-align: top;
    }
    span {
      display: block;
      width: 50%;
      margin: 0 auto;
      height: 2px;

      background-image: -webkit-gradient(linear, 0% 50%, 100% 50%, color-stop(0%, #ffffff), color-stop(50%, #000000), color-stop(100%, #ffffff));
      background-image: -webkit-linear-gradient(left, #ffffff 0%, #000000 50%, #ffffff 100%);
      background-image: -moz-linear-gradient(left, #ffffff 0%, #000000 50%, #ffffff 100%);
      background-image: -o-linear-gradient(left, #ffffff 0%, #000000 50%, #ffffff 100%);
      background-image: linear-gradient(left, #ffffff 0%, #000000 50%, #ffffff 100%);
    }

  </style>
</head>

<body>

  <div class="center">
    <img src="http://www.turniton.co.uk/templates/turniton/images/logo.png" alt="">
    <form action="/search" method="GET">
      <input type="text" placeholder="Enter your search term" name="q" value="<%= params['q'] || '' %>">
      <button type="submit">Go!</button>
    </form>
  </div>

  <span></span>

  <%= yield %>
</body>

</html>